import Layout from "../components/Layout"

const Order= ()=>{
    return(
        <Layout>
            <div>
                No order so far!
            </div>
        </Layout>
    )
}

export default Order;